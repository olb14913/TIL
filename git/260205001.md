# 오늘 배운 것

## 1. pre-commit
- commit 전에 체크해야 할 것들을 자동 수행하도록 도와주는 도구
- $pip install pre-commit > $pre-commit --version > touch .pre-commit-config.yaml

## 2. branch

### 2.1. branch 확인
```git branch```

### 2.2. branch 생성
```git branch {branch_name}

### 2.3. branch 전환
```git switch {branch_name}

### 2.4. branch 삭제 
```git branch -D {branch_name}

## Conflict 
- merge > vi를 통해 충돌파일(ex : <<<<<<,======) 편집 및 저장 > git add {file_name} > git commit
- rebase > git rebase main > vi > git add > git rebase --continue

## github flow 숙지를 위한 fizzbuzz 실습
- github.com에서 new repository 생성
	- Repository name : github_flow
	- Add README : O
	- Add licence : MIT License
- 이슈(markdown) 생성 
	- 프로젝트 환경설정 
		- Description : 프로젝트를 진행하기 위한 환경설정을 실시합니다.
		- Tasks(to do) 
			- .gitignore 생성
			- README.md 작성
		- Assignees : olb14913
		- Labels : documentation, enhancement 
	- fizz 구현
		- Description  
			- fizzbuzz 전체 내용 중 fizz에 대한 구현 입니다.
			- 임의의 양의 정수 i가 3의 배수라면, fizz를 출력해야 합니다.
			- 그렇지 않은 경우, i를 출력합니다.
		- Tasks(to do)
			- fb.py 생성
			- 반복문, 조건문을 활용한 fizz 출력 구현(i:1~15)
			- Assignees : olb14913 
			- Labels : enhancement
	- buzz 구현
		- Destiption : 5의 배수일 경우 buzz를 구현합니다.
		- Tasks(to do) : buzz 출력 코드 구현 
		- Assignees : olb14913
		- Labels : enhancement
	- fizzbuzz 구현 
		- Destiption : 15의 배수일 경우 fizzbuzz 를 출력하는 코드를 구현합니다.
		- Tasks(to do) : buzz 출력 코드 구현 
		- Assignees : olb14913
		- Labels : enhancement
- Teminal 이동
- ~/Documents/dev > $git clone {repository_url} > ls (디렉토리 확인)
- $cd {repository_name}
- $touch .gitignore > $vi .gitignore > $git add .gitignore > $git commit > $git push origin main 
	- [gitignore(macos,vim,python)](https://www.toptal.com/developers/gitignore/api/macos,vim,python)
	- commit message 
		- config : Create .gitignore
- $vi README.md > $git add README.md > $git commit > $git push origin main
	- commit message 
		- docs : Uadate README.md
- $git branch(브랜치 확인) > $git branch (feature/fizz)
- $touch fb.py > $vi fb.py > $python3 fb.py > $git add fb.py > $git commit > $git push -u origin feature/fizz 
```
for i in range(1, 15+1):
		if i % 3 == 0:
			print('fizz')
		else:
			print(i)
```
	- commit message 
		- feat : Add fizz function
- github로 이동
- [compare&pull request] btn 클릭 > "close #n"를 통해 pull request와 issue 연결 
- [Merge Pull Request] btn 클릭 > [Delete branch] btn 클릭 
- Teminal로 다시 돌아오기 
- $git switch main > $cat fb.py (원본 파일 확인, 필수 아님) > $git fetch origin main > $git merge FETCH_HEAD 
- $cat fb.py(merge된 내용 확인, 필수 아님) > $git branch -D feature/fizz 
- buzz, fizzbuzz 이슈에 대해 branch 생성 작업부터 branch 삭제 작업까지 반복

		
